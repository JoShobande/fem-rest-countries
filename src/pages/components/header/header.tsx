import { StyledHeader } from "./styles/header.styled";

const Header = () => {
    return(
        <StyledHeader>
            <h1>Where in the world?</h1>
        </StyledHeader>
    )
}

export default Header;